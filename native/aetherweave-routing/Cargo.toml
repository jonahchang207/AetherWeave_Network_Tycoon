[package]
name = "aetherweave-routing"
version = "0.1.0"
edition = "2021"
description = "Routing and flow simulation for AetherWeave (Unity native plugin)"
license = "MIT OR Apache-2.0"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]

[dev-dependencies]
