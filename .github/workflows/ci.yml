name: CI

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

jobs:
  routing-tests:
    name: Routing & flow (Rust)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: native/aetherweave-routing
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo test --verbose
      - run: cargo build --release

  # Enable when a Unity license / game-ci image is available:
  # unity-build:
  #   name: Unity build
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: game-ci/unity-builder@v4
  #       with:
  #         projectPath: unity/AetherWeave
  #         targetPlatform: StandaloneLinux64
